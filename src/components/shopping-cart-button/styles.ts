import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  buttonContainer: {
    paddingHorizontal: 14,
  },
  buttonText: {
    position: 'absolute',
    top: -5,
    left: 5,
  },
});
